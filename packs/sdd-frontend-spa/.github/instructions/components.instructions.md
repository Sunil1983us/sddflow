---
applyTo: "src/**/*.tsx,src/**/*.vue,src/**/*.component.ts"
---
Max 150 lines — split if larger
No API calls — use service layer
No inline styles — use project styling solution
Typed props always — no any
Every component has paired test
