# Data Model Document
# Feature: {Feature Name}

> Version: 1.0 | Status: Draft | Date: {date}
> Input: arch.summary.md

---

## 1. Overview
{One paragraph — two tables, Flyway managed, PostgreSQL.}

## 2. Entity Relationship
```
payments (1) ──── (N) payment_status_history
    payment_id (PK)        payment_id (FK)
```

## 3. Table: payments

| Column | Type | Nullable | Notes |
|---|---|---|---|
| payment_id | UUID | NOT NULL | PK — generated by ICS |
| correlation_id | VARCHAR(50) | NOT NULL | YYYYMMDD-OB-NNNNNN |
| status | VARCHAR(30) | NOT NULL | Current payment status |
| pacs008_xml | TEXT | NOT NULL | Full pacs.008 received |
| clearing_ref | VARCHAR(50) | NULL | From CSM response |
| amount | DECIMAL(19,4) | NOT NULL | Payment amount |
| currency | VARCHAR(3) | NOT NULL | Always EUR in pilot |
| debtor_bic | VARCHAR(11) | NOT NULL | From pacs.008 |
| creditor_bic | VARCHAR(11) | NOT NULL | From pacs.008 |
| transaction_id | VARCHAR(35) | NOT NULL | From pacs.008 TransactionId |
| acceptance_dt | TIMESTAMP | NOT NULL | From pacs.008 AcceptanceDateTime |
| msg_id | VARCHAR(35) | NOT NULL | From pacs.008 MsgId |
| created_at | TIMESTAMP | NOT NULL | UTC |
| updated_at | TIMESTAMP | NOT NULL | UTC |

**Indexes:**
- `idx_payments_correlation_id` ON payments(correlation_id)
- `idx_payments_status` ON payments(status)
- `idx_payments_transaction_id` ON payments(transaction_id)

## 4. Table: payment_status_history

| Column | Type | Nullable | Notes |
|---|---|---|---|
| id | UUID | NOT NULL | PK |
| payment_id | UUID | NOT NULL | FK → payments.payment_id |
| from_status | VARCHAR(30) | NULL | Previous status |
| to_status | VARCHAR(30) | NOT NULL | New status |
| changed_at | TIMESTAMP | NOT NULL | UTC |
| notes | TEXT | NULL | Optional context |

**Indexes:**
- `idx_psh_payment_id` ON payment_status_history(payment_id)

## 5. Payment Status Enum
```java
public enum PaymentStatus {
    RECEIVED,
    VALIDATION,
    FRAML_CHECK,
    FUNDS_RESERVED,
    AWAITING_RECEIPT,
    RECONCILING,
    SETTLED      // only terminal status in pilot
}
```

## 6. Flyway Migration Scripts
| Script | Purpose |
|---|---|
| V001__create_payments_table.sql | Create payments table + indexes |
| V002__create_payment_status_history_table.sql | Create history table + indexes |

## 7. Data Dictionary
| Field | Business Meaning |
|---|---|
| payment_id | Unique ICS-generated identifier. Propagated as X-Tracking-Id |
| correlation_id | End-to-end tracing key from Gateway. On every log line |
| clearing_ref | RT1 AGI acknowledgement reference from CSM Service |
| transaction_id | pacs.008 TransactionId — used in reconciliation |
| acceptance_dt | pacs.008 AcceptanceDateTime — used in reconciliation |
| msg_id | pacs.008 MsgId — used in reconciliation |

---
*Generated from: arch.summary.md*
