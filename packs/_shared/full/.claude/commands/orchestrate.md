---
description: ORCHESTRATE — Drive the full SDLC pipeline as a sequenced multi-agent workflow. Pauses at every human gate. Supports CLI (single-session) and SDK (multi-agent) modes. Use --list to view pipeline status, --from STEP to resume, --to STEP to stop early.
argument-hint: [--from STEP] [--to STEP] [--list] [--mode multi-agent]
---

Read and follow ALL instructions in `.github/prompts/orchestrate.prompt.md` exactly as written, then execute them now.

$ARGUMENTS
