---
applyTo: "**/*.test.ts,**/*.spec.ts,**/*.test.tsx"
---
Testing Library — no implementation details
Naming: should_{expected}_when_{condition}
Mock all API calls — no real network
axe-core a11y check on every component test
No snapshot tests — assert behaviour not markup
