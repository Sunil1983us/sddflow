---
applyTo: "**/services/**,**/api/**"
---
All API calls here — never in screens
Handle offline state — queue if no network
Typed request + response — no any
No UI logic — pure data + storage functions
